export type Credentials = {
  email: string,
  username: string,
  userId: string,
  isAdmin: boolean,
  token: string
}

export type CatInfo = {
  id: string,
  name: string,
  race: string,
  sexe: string,
  city: string,
  birthday: string,
  description: string,
  picture: string,
  status: string,
  requestUserId: string,
}