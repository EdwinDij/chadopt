export type Credentials = {
  email: string,
  username: string,
  userId: string,
  isAdmin: boolean,
  token: string
}

export type CatInfo = {
  name: string,
  age: number,
  race: string,
  sexe: string,
  city: string,
  description: string,
  photo: string,
  status: string,
}